There are 3 ways of testing is represented:

- test with mocha (mocha-testing folder)
- test with jest (jest-testing folder)
- the code on vanilla js (vanilla-js folder)

All the implementations do the same logic.
